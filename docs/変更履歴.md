# 変更履歴

## 2026-09-23 — V01R01 Google Cloud移植版

Firebase Hostingへ公開し、全利用者の認証をGoogleへ統一しました。オーナー・管理者もGoogleでログインし、サーバー側のカスタムクレームで権限を区別します。Supabaseのデータ4件とGoogle認証アカウント1件を移行し、公開投稿と非公開所有者情報をFirestoreで分離しました。

無料のSparkプランを使用し、課金アカウントは接続していません。旧GitHub Pagesは新しい公開先への案内に置き換えます。`V01R01` の固定ベースラインは変更せず、新しいチェックポイントを別名で残します。

## 移行前の履歴

元の変更履歴と資料は [history/pre-firebase](history/pre-firebase/) に保存しています。
