# Migration is incomplete. This is NOT a runnable deployment configuration.
# The reconstructed map uses this browser-visible key for the original proxy.
# Availability on a new domain is unverified. Select a supported map service first.
# Never put server secrets or database admin keys in VITE_* variables.
VITE_MAPS_PROXY_KEY=
